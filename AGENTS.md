WE MUST NOT WORK AROUND TOOLCRAFT ISSUES


Any issue must fixed in toolcraft itself, so we improve the library. 

Code here
/Users/kjopek/Workspace/poe-code/packages/toolcraft/

You can make fixes, but make sure you push the poe-code and wait for release

## Debugging

Use playwright cli to investigate

Prompt user to login and pause, wait for user to login

Explore the api calls to determine fix

## Workflow

Every work package/task must result in commit, push into main, wait for release. If push blocked - open pull request.